{
    // Point this at YOUR vcpkg install (the folder containing vcpkg.exe).
    // Edit the path below, then run "CMake: Configure" from the CMake
    // Tools sidebar - no terminal needed.
    "cmake.configureSettings": {
        "CMAKE_TOOLCHAIN_FILE": "C:/Program Files (x86)/Microsoft Visual Studio/2022/BuildTools/VC/vcpkg/scripts/buildsystems/vcpkg.cmake",
        "VCPKG_TARGET_TRIPLET": "x64-windows"
    },
    "cmake.buildDirectory": "${workspaceFolder}/build"
}